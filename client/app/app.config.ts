export default defineAppConfig({
  ui: {
    button: {
      base: "cursor-pointer",
      compoundVariants: [
        {
          variant: "solid",
          class: "text-white",
        },
      ],
    },
  },
});
